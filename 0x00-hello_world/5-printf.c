#include <stdio.h>
/**
 * main - this is used to print with proper grammar,
 * but the outcome is a piece of art,
 *
 * Return: the function would return 0 if it prints
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
